{
  "name": "@zversal/demo-module",
  "version": "1.0.0",
  "description": "This is a nodejs npm package demo module",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && babel src/lib --out-dir dist --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "dependencies": {
    "@babel/cli": "^7.21.0"
  }
}
